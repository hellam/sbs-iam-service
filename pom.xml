<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.10</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>
    <groupId>ke.shiva</groupId>
    <artifactId>iam-service</artifactId>
    <version>1.0.1-SNAPSHOT</version>
    <name>iam-service</name>
    <description>Identity and Access Management Service</description>
    <url/>
    <licenses>
        <license>
            <name>Proprietary License</name>
            <url>https://shiva.co.ke/licenses/proprietary</url>
            <distribution>repo</distribution>
        </license>
    </licenses>
    <developers>
        <developer/>
    </developers>
    <scm>
        <connection/>
        <developerConnection/>
        <tag/>
        <url/>
    </scm>
    <properties>
        <java.version>21</java.version>
        <spring-cloud.version>2025.0.0</spring-cloud.version>
        <lombok.version>1.18.34</lombok.version>
        <jib-maven-plugin.version>3.5.1</jib-maven-plugin.version>
        <jib.from.image>ghcr.io/hellam/shiva/java-runtime-base:21</jib.from.image>
        <jib.to.image>shiva/${project.artifactId}:latest</jib.to.image>
    </properties>
    <repositories>
        <repository>
            <id>github-shiva-core</id>
            <name>Shiva Spring Core Starter</name>
            <url>https://maven.pkg.github.com/hellam/shiva-spring-core-starter</url>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>
    </repositories>
    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-mail</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-circuitbreaker-resilience4j</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.liquibase</groupId>
            <artifactId>liquibase-core</artifactId>
        </dependency>
        <dependency>
            <groupId>me.paulschwarz</groupId>
            <artifactId>spring-dotenv</artifactId>
            <version>3.0.0</version>
        </dependency>
        <dependency>
            <groupId>ke.shiva.corestarter</groupId>
            <artifactId>shiva-spring-core-starter</artifactId>
            <version>1.0.36</version>
        </dependency>
        <!-- Notification Client for sending notifications -->
        <dependency>
            <groupId>ke.shiva</groupId>
            <artifactId>notification-client</artifactId>
            <version>1.0.0</version>
        </dependency>
        <dependency>
            <groupId>ke.shiva</groupId>
            <artifactId>iam-client</artifactId>
            <version>1.0.0</version>
        </dependency>
        <dependency>
            <groupId>ke.shiva</groupId>
            <artifactId>account-client</artifactId>
            <version>1.0.0</version>
        </dependency>
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.7.0</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-authorization-server</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.kafka</groupId>
            <artifactId>spring-kafka</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
        </dependency>
        <dependency>
            <groupId>com.vladmihalcea</groupId>
            <artifactId>hibernate-types-60</artifactId> <!-- If on Spring Boot 3 -->
            <version>2.21.1</version>
        </dependency>
        <!-- MaxMind GeoIP2 for IP geolocation -->
        <dependency>
            <groupId>com.maxmind.geoip2</groupId>
            <artifactId>geoip2</artifactId>
            <version>4.2.0</version>
        </dependency>
        <!-- Bucket4j for rate limiting -->
        <dependency>
            <groupId>com.bucket4j</groupId>
            <artifactId>bucket4j-core</artifactId>
            <version>8.7.0</version>
        </dependency>
        <dependency>
            <groupId>com.bucket4j</groupId>
            <artifactId>bucket4j-redis</artifactId>
            <version>8.7.0</version>
        </dependency>
        <!-- Lettuce Redis client (for Bucket4j Redis integration) -->
        <dependency>
            <groupId>io.lettuce</groupId>
            <artifactId>lettuce-core</artifactId>
        </dependency>
    </dependencies>
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.liquibase</groupId>
                <artifactId>liquibase-maven-plugin</artifactId>
                <version>4.31.1</version>

                <!-- This liquibase configuration is for running migrations from Maven only.
                     The application itself runs migrations on startup using the Spring Boot integration.
                     Remove this section in production for security reasons.
                     -->
                <configuration>
                    <changeLogFile>src/main/resources/db/changelog/db.changelog-master.yaml</changeLogFile>
                    <url>jdbc:postgresql://localhost:5432/shiva_banking</url>
                    <username>hellam</username>
                    <password>Hellix1425</password>
                    <driver>org.postgresql.Driver</driver>
                    <defaultSchemaName>iam_service</defaultSchemaName>
                    <changelogSchemaName>public</changelogSchemaName>
                </configuration>
            </plugin>
            <plugin>
                <groupId>com.google.cloud.tools</groupId>
                <artifactId>jib-maven-plugin</artifactId>
                <version>${jib-maven-plugin.version}</version>
                <configuration>
                    <from>
                        <image>${jib.from.image}</image>
                    </from>
                    <to>
                        <image>${jib.to.image}</image>
                    </to>
                    <container>
                        <appRoot>/app</appRoot>
                        <entrypoint>
                            <entry>/usr/local/bin/java-entrypoint.sh</entry>
                        </entrypoint>
                        <user>10001:10001</user>
                        <workingDirectory>/app</workingDirectory>
                        <environment>
                            <SHIVA_SECRETS_DIR>/run/shiva-secrets</SHIVA_SECRETS_DIR>
                        </environment>
                    </container>
                    <extraDirectories>
                        <paths>
                            <path>
                                <from>${project.basedir}/../../deployment/docker</from>
                                <into>/usr/local/bin</into>
                                <includes>
                                    <include>java-entrypoint.sh</include>
                                </includes>
                            </path>
                        </paths>
                        <permissions>
                            <permission>
                                <file>/usr/local/bin/java-entrypoint.sh</file>
                                <mode>755</mode>
                            </permission>
                        </permissions>
                    </extraDirectories>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
