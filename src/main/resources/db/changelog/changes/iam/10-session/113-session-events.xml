<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
          http://www.liquibase.org/xml/ns/dbchangelog
          http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <!-- 113 - SESSION EVENTS -->

    <changeSet id="pg-iam-113-session-events" author="hellamimbosa">

        <createTable tableName="session_events">

            <column name="id" type="BIGSERIAL">
                <constraints primaryKey="true" nullable="false"
                             primaryKeyName="pk_session_events"/>
            </column>

            <column name="session_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>

            <!-- CREATED, REFRESHED, REVOKED, EXPIRED, MFA_REQUIRED, MFA_PASSED, etc -->
            <column name="event_type" type="VARCHAR(50)">
                <constraints nullable="false"/>
            </column>

            <column name="event_at" type="TIMESTAMPTZ"
                    defaultValueDate="CURRENT_TIMESTAMP"/>

            <column name="ip_address" type="VARCHAR(50)"/>
            <column name="device_id" type="VARCHAR(255)"/>

            <column name="metadata" type="JSONB"/>

        </createTable>

        <addForeignKeyConstraint
                baseTableName="session_events" baseColumnNames="session_id"
                referencedTableName="sessions" referencedColumnNames="id"
                constraintName="fk_session_events_session"/>

        <createIndex tableName="session_events" indexName="idx_session_events_session">
            <column name="session_id"/>
        </createIndex>

    </changeSet>

</databaseChangeLog>
