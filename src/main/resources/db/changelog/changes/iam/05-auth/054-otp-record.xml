<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
          http://www.liquibase.org/xml/ns/dbchangelog
          http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="pg-iam-054-otp-record" author="hellamimbosa">

        <createTable tableName="otp_record" schemaName="iam_service">

            <column name="id" type="BIGSERIAL">
                <constraints primaryKey="true" nullable="false"
                             primaryKeyName="pk_otp_record"/>
            </column>

            <column name="channel" type="VARCHAR(50)">
                <constraints nullable="false"/>
            </column>

            <column name="notification_channel" type="VARCHAR(50)">
                <constraints nullable="false"/>
            </column>

            <!-- JWT jti or internal session ID -->
            <column name="session_id" type="VARCHAR(100)">
                <constraints nullable="false" unique="true"/>
            </column>

            <column name="otp_hash" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>

            <column name="expiry_time" type="TIMESTAMPTZ">
                <constraints nullable="false"/>
            </column>

            <column name="verify_attempts" type="SMALLINT" defaultValueNumeric="0"/>

            <!-- Status: PENDING, VERIFIED -->
            <column name="status" type="VARCHAR(20)" defaultValue="PENDING"/>

            <column name="to" type="VARCHAR(255)"/>

            <column name="created_at" type="TIMESTAMPTZ"
                    defaultValueDate="CURRENT_TIMESTAMP"/>
            <column name="updated_at" type="TIMESTAMPTZ"
                    defaultValueDate="CURRENT_TIMESTAMP"/>

        </createTable>

        <addUniqueConstraint tableName="otp_record" schemaName="iam_service"
                             columnNames="session_id"
                             constraintName="uc_otp_record_session_id"/>

        <createIndex indexName="idx_otp_record_session_id"
                     tableName="otp_record" schemaName="iam_service">
            <column name="session_id"/>
        </createIndex>

        <createIndex indexName="idx_otp_record_status"
                     tableName="otp_record" schemaName="iam_service">
            <column name="status"/>
        </createIndex>

    </changeSet>

</databaseChangeLog>
