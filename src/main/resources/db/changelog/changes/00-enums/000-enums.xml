<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
            http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <!-- ============================================================= -->
    <!-- ENUM TYPES FOR IAM SYSTEM                                      -->
    <!-- PostgreSQL-specific ENUMs used across all IAM tables           -->
    <!-- ============================================================= -->

    <changeSet id="pg-enums-000" author="hellamimbosa">

        <comment>
            ENUM definitions for PARTY, IAM, ORGANIZATION, IDENTIFIERS,
            EMPLOYEES, and ORGANIZATION USERS.
            This migration must run BEFORE any table definitions.
        </comment>

        <!-- ======================== -->
        <!-- PARTY ENUMS              -->
        <!-- ======================== -->

        <sql>
            DO
            $$
            BEGIN
            CREATE TYPE party_type_enum AS ENUM ('PERSON', 'ORGANIZATION');
            EXCEPTION WHEN duplicate_object THEN NULL;
            END $$;
        </sql>

        <sql>
            DO
            $$
            BEGIN
            CREATE TYPE party_status_enum AS ENUM ('ACTIVE', 'INACTIVE', 'BLOCKED', 'DORMANT', 'CLOSED');
            EXCEPTION WHEN duplicate_object THEN NULL;
            END $$;
        </sql>

        <sql>
            DO
            $$
            BEGIN
            CREATE TYPE gender_enum AS ENUM ('MALE', 'FEMALE', 'OTHER');
            EXCEPTION WHEN duplicate_object THEN NULL;
            END $$;
        </sql>


        <!-- ======================== -->
        <!-- CUSTOMER & ORGANIZATION   -->
        <!-- ======================== -->

        <sql>
            DO
            $$
            BEGIN
            CREATE TYPE customer_type_enum AS ENUM ('RETAIL', 'RETAIL_PRIORITY');
            EXCEPTION WHEN duplicate_object THEN NULL;
            END $$;
        </sql>

        <sql>
            DO
            $$
            BEGIN
            CREATE TYPE org_segment_enum AS ENUM ('SME', 'CORPORATE');
            EXCEPTION WHEN duplicate_object THEN NULL;
            END $$;
        </sql>

        <sql>
            DO
            $$
            BEGIN
            CREATE TYPE sme_mode_enum AS ENUM (
                    'SOLE_PROPRIETOR',
                    'PARTNERSHIP',
                );
            EXCEPTION WHEN duplicate_object THEN NULL;
            END $$;
        </sql>


        <!-- ======================== -->
        <!-- IAM USER ENUMS           -->
        <!-- ======================== -->

        <sql>
            DO
            $$
            BEGIN
            CREATE TYPE user_category_enum AS ENUM (
                    'CUSTOMER',
                    'EMPLOYEE',
                    'AGENT',
                    'MERCHANT',
                    'SYSTEM'
                );
            EXCEPTION WHEN duplicate_object THEN NULL;
            END $$;
        </sql>

        <sql>
            DO
            $$
            BEGIN
            CREATE TYPE auth_provider_enum AS ENUM (
                    'LOCAL',
                    'LDAP',
                    'OIDC',
                    'SAML'
                );
            EXCEPTION WHEN duplicate_object THEN NULL;
            END $$;
        </sql>

        <sql>
            DO
            $$
            BEGIN
            CREATE TYPE iam_status_enum AS ENUM (
                    'ACTIVE',
                    'INACTIVE',
                    'LOCKED',
                    'SUSPENDED'
                );
            EXCEPTION WHEN duplicate_object THEN NULL;
            END $$;
        </sql>


        <!-- ======================== -->
        <!-- LOGIN IDENTIFIER ENUMS    -->
        <!-- ======================== -->

        <sql>
            DO
            $$
            BEGIN
            CREATE TYPE channel_enum AS ENUM (
                    'INTERNET_BANKING',
                    'MOBILE_BANKING',
                    'BACKOFFICE',
                    'AGENT_APP',
                    'MERCHANT_PORTAL',
                    'API_ACCESS'
                );
            EXCEPTION WHEN duplicate_object THEN NULL;
            END $$;
        </sql>

        <sql>
            DO
            $$
            BEGIN
            CREATE TYPE identifier_type_enum AS ENUM (
                    'USERNAME',
                    'EMAIL',
                    'PHONE'
                );
            EXCEPTION WHEN duplicate_object THEN NULL;
            END $$;
        </sql>


        <!-- ======================== -->
        <!-- ORGANIZATION USER ENUMS  -->
        <!-- ======================== -->

        <sql>
            DO
            $$
            BEGIN
            CREATE TYPE org_user_role_enum AS ENUM (
                    'VIEW_ONLY',
                    'MAKER',
                    'CHECKER',
                    'APPROVER',
                    'ADMIN'
                );
            EXCEPTION WHEN duplicate_object THEN NULL;
            END $$;
        </sql>

        <sql>
            DO
            $$
            BEGIN
            CREATE TYPE org_user_status_enum AS ENUM (
                    'ACTIVE',
                    'INACTIVE',
                    'BLOCKED',
                    'PENDING'
                );
            EXCEPTION WHEN duplicate_object THEN NULL;
            END $$;
        </sql>


        <!-- ======================== -->
        <!-- EMPLOYEE ENUMS           -->
        <!-- ======================== -->

        <sql>
            DO
            $$
            BEGIN
            CREATE TYPE employment_status_enum AS ENUM (
                    'ACTIVE',
                    'SUSPENDED',
                    'TERMINATED',
                    'ON_LEAVE'
                );
            EXCEPTION WHEN duplicate_object THEN NULL;
            END $$;
        </sql>

        <!-- ======================== -->
        <!-- BRANCH ENUMS           -->
        <!-- ======================== -->

        <sql>
            DO
            $$
            BEGIN
            CREATE TYPE branch_type_enum AS ENUM (
                    'BRANCH',
                    'SUB_BRANCH',
                    'ATM',
                );
            EXCEPTION WHEN duplicate_object THEN NULL;
            END $$;
        </sql>
        <!-- ======================== -->
        <!-- FEATURE POLICY ENUMS           -->
        <!-- ======================== -->

        <sql>
            DO
            $$
            BEGIN
            CREATE TYPE feature_policy_scope_enum AS ENUM (
                    'GLOBAL',
                    'ORGANIZATION',
                    'USER'
                );
            EXCEPTION WHEN duplicate_object THEN NULL;
            END $$;
        </sql>
        <!-- ======================== -->
        <!-- FEATURE POLICY ENUMS           -->
        <!-- ======================== -->

        <sql>
            DO
            $$
            BEGIN
            CREATE TYPE security_events_status_enum AS ENUM (
                    'NEW',
                    'IN_REVIEW',
                    'CLOSED',
                    'DISMISSED'
                );
            EXCEPTION WHEN duplicate_object THEN NULL;
            END $$;
        </sql>

    </changeSet>

</databaseChangeLog>
